[package]
name = "sts-core"
version = "2.2.0"
description = "星TAP 极速搜索核心引擎 - 不依赖 Tauri，可独立被 CLI / AI / Skill 调用"
authors = ["星TAP实验室 <cscb603@qq.com>"]
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.0", features = ["full"] }
